namespace Nexuscomm.API.Enums
{
    // Status of a single dispatch attempt (used in CommunicationAttempt records)
    public enum AttemptStatus
    {
        Success = 0,
        Failed = 1
    }
}